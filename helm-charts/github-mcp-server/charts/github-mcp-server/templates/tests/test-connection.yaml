apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "github-mcp-server.fullname" . }}-test-connection"
  labels:
    {{- include "github-mcp-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "github-mcp-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
