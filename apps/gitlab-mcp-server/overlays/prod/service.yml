---
apiVersion: v1
kind: Service
metadata:
  name: gitlab-mcp-server
  namespace: mcp-servers-prod
spec:
  selector:
    app: gitlab-mcp-server
  ports:
    - name: http
      port: 80
      targetPort: 3002
  type: ClusterIP

