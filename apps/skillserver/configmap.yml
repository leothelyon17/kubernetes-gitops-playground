apiVersion: v1
kind: ConfigMap
metadata:
  name: skillserver-config
  namespace: mcp-servers-dev
  labels:
    app.kubernetes.io/name: skillserver
    app.kubernetes.io/instance: skillserver-dev
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: mcp-servers
data:
  SKILLSERVER_DIR: "/data/skills"
  SKILLSERVER_PORT: "8080"
  # In Kubernetes we want logs available through pod logs.
  SKILLSERVER_ENABLE_LOGGING: "true"
