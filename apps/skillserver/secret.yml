apiVersion: v1
kind: Secret
metadata:
  name: skillserver-secrets
  namespace: mcp-servers-dev
  labels:
    app.kubernetes.io/name: skillserver
    app.kubernetes.io/instance: skillserver-dev
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: mcp-servers
type: Opaque
stringData:
  # Optional comma-separated git repositories to sync.
  # Example: "https://github.com/anthropics/skills.git"
  SKILLSERVER_GIT_REPOS: ""
