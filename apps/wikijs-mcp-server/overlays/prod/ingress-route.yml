---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wikijs-mcp-prod-ingressroute
  namespace: mcp-servers-prod
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`wikijs-mcp.home.nerdylyonsden.io`)
      kind: Rule
      services:
        - name: wikijs-mcp-server
          port: 80
  tls:
    secretName: prod-apps-certificate-secret
