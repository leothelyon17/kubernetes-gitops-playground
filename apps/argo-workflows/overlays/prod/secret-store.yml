apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: vault-backend-argo-workflows-prod
  namespace: argo-workflows-prod
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  provider:
    vault:
      server: "http://hashi-vault-prod-0.hashi-vault-prod-internal.hashi-vault-prod.svc.cluster.local:8200"
      path: "secret"
      version: "v2"
      auth:
        kubernetes:
          mountPath: "kubernetes"
          role: "argo-workflows"
          serviceAccountRef:
            name: argo-workflows-prod-workflow-controller
            namespace: argo-workflows-prod
