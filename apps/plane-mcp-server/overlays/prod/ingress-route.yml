---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: plane-mcp-prod-ingressroute
  namespace: mcp-servers-prod
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`plane-mcp.home.nerdylyonsden.io`)
      kind: Rule
      services:
        - name: plane-mcp-server
          port: 80
  tls:
    secretName: prod-apps-certificate-secret
