apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-cert-prod
spec:
  secretName: prod-apps-certificate-secret
  commonName: "*.nerdylyonsden.io"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
  dnsNames:
  - "*.nerdylyonsden.io"

  secretTemplate:
    annotations:
      reflector.v1.k8s.emberstack.com/reflection-allowed: "true"
      reflector.v1.k8s.emberstack.com/reflection-allowed-namespaces: "nautobot-prod"  # Control destination namespaces
